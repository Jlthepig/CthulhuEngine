#version 430 core
out vec4 FragColor;

in vec3 FragPos;

uniform vec3 lightPos;
uniform float farPlane;

void main()
{
    float lightDistance = length(FragPos - lightPos);
    FragColor = vec4(lightDistance / farPlane, 0.0, 0.0, 1.0);
}